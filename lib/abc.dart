import 'package:flutter/material.dart';

class PracWidg extends StatelessWidget {
  
  const PracWidg({
    super.key,

  });

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}